# W90 MAX78000

Use these wrappers for the width-reduced `kws20_demo_w90` firmware.

Main commands:

- `./build_flash_live.sh`
- `./measure_live.sh`
- `./build_flash_offline.sh`
- `./measure_offline.sh`

Optional:

- `./run_accuracy.sh`

The shared implementation lives one directory up in `../`.
