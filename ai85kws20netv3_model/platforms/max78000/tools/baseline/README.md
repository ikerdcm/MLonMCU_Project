# Baseline MAX78000

Use these wrappers for the original `kws20_demo` firmware.

Main commands:

- `./build_flash_live.sh`
- `./measure_live.sh`
- `./build_flash_offline.sh`
- `./measure_offline.sh`

Optional:

- `./run_accuracy.sh`

The shared implementation lives one directory up in `../`.
